---
import ChecklistActivities from '../components/Content/ChecklistActivities.astro'

const { content, githubEditUrl } = Astro.props;
const title = content.title;
const headers = content.astro ? content.astro.headers : undefined;
---

<article id="article" class="content">
	<section class="main-section">
		<nav class="block sm:hidden">
		</nav>
		<slot />
		<ChecklistActivities onMainPage/>
	</section>
</article>