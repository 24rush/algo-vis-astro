---
title: "Exercitii Tipuri de date"
layout: "../../layouts/MainLayout.astro"
---

import InlineEditor from '../../components/Content/InlineEditor.astro'

<ol class="exercise-list">
    1. Să se declare variabila <code>numarJucatori</code> ce conține valoarea 0.<InlineEditor exercise="tipuri-date.json:1"></InlineEditor> 
    2. Să se declare constanta <code>MAX_JUCATORI</code> ce conține valoarea 10.<InlineEditor exercise="tipuri-date.json:2"></InlineEditor>
    3. Să se declare variabila obiect cu numele <code>stareJoc</code> ce conține următoarele proprietăți și valori:
        ```
        - nivel : 1
        - scor : 163
        - areSuperPower : true
        ```
        <InlineEditor exercise="tipuri-date.json:3"></InlineEditor>
    4. În obiectul <code>stareJoc</code> creat, să se schimbe valoarea proprietății <code>areSuperPower</code> în false.<InlineEditor exercise="tipuri-date.json:4"></InlineEditor>
    5. Să se declare o variabila booleană <code>x</code> a cărei valoare să fie false iar apoi să se declare una nouă <code>y</code> care să conțină valoarea celei inițial create (<code>y</code>).<InlineEditor exercise="tipuri-date.json:5"></InlineEditor>
    6. Să se declare o variabilă <code>numeJucator</code> care să conțină numele tău.<InlineEditor exercise="tipuri-date.json:6"></InlineEditor>
</ol>