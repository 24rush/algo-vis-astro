---
title: "Exercitii Tipuri de date"
layout: "../../layouts/MainLayout.astro"
---
import InlineEditor from '../../components/Content/InlineEditor.astro'

<ol class="exercise-list">
1. Să se solicite un număr real <code>x</code> de la utilizator apoi să se calculeze funcția:
    ![](../assets/imgs/operatori_1.png)
    <InlineEditor type="ieditor" av_exercise="structuri.json:1"></InlineEditor>
2. Să se citească două numere n și x de la tastatură iar apoi să se calculeze funcția:
    ![](../assets/imgs/structuri_1.png)
    > Atenție: valori mari pentru variabila _n_ vor duce la blocarea aplicației
    <InlineEditor type="ieditor" av_exercise="structuri.json:2"></InlineEditor>
3. Să se genereze numerele șirului Fibonacci până la o valoare <code>n</code> naturală citită de la tastatură.
Un șir Fibonacci este definit astfel:
    ![](../assets/imgs/fibonacci.png)
    > Exemplu pentru n = 6, Fib(6) = 0, 1, 1, 2, 3, 5
    <InlineEditor type="ieditor" av_exercise="structuri.json:3"></InlineEditor>

4. Să se citească două valori <code>n</code> și <code>p</code> de la tastatură iar apoi <code>n</code> numere. Să se afișeze după citirea celor <code>n</code> numere câte numere divizibile cu <code>n</code> au fost introduse de utilizator.<InlineEditor type="ieditor" av_exercise="structuri.json:4"></InlineEditor>
5. Să se citească 6 numere de la tastatură într-un vector iar apoi prin parcurgerea vectorului să se afișeze câte alternări de la numere pozitive la numere negative și invers am avut în lista de numere.
    > Indiciu: pentru a determina o alternare de semn putem să înmulțim cele două valori adiacente iar dacă rezultatul este negativ atunci clar unul din numere este pozitiv iar celălalt negativ
    <InlineEditor type="ieditor" av_exercise="structuri.json:5"></InlineEditor>

8. Să se genereze toate numerele de 3 cifre ce au toate cifrele în ordine crescătoare.
   > Exemplu: 123, 124, 456, 367, etc.
   <InlineEditor type="ieditor" av_exercise="structuri.json:7"></InlineEditor>

9. Se definește următoarea transformare pentru un număr natural n:
    ![](../assets/imgs/collatz.png)    

    >Se știe că dacă se va aplica această formulă repetat asupra valorii lui n, se va ajunge la n = 1. Să se determine acest număr de pași pentru un număr natural n citit de la tastatură
    > Exemplu: n = 12, Numar iteratii: 9 (adica 12 3 10 5 16 8 4 2 1)
    <InlineEditor type="ieditor" av_exercise="structuri.json:8"></InlineEditor>
</ol>